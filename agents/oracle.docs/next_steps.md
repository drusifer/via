# Oracle Next Steps

## Pipeline TLDR Sweep
1. Update `via/pipeline/parser.py`, `executor.py`, `relationship_filter.py`.
2. Verify each summary is single-line.

## Service & Web Sweep
1. Batch update `via/services/` and `via/web/` files.
